import { MatDatePickerDirective } from './mat-date-picker.directive';

describe('MatDatePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new MatDatePickerDirective();
    expect(directive).toBeTruthy();
  });
});
