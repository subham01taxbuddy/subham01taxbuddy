function we_login(userId) {
          webengage.user.login(userId);
}
function we_setAttribute(key, value) {
          webengage.user.setAttribute(key, value);
}
function we_track(key, value) {
          webengage.track(key, value);
}